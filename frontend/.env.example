# 复制为 `.env.development` 或 `.env.local` 后按需修改（勿提交密钥）。
#
# 本机常见端口（勿混淆）：
# - Perilla FastAPI 网关：默认监听 8000；前端须指向「正在运行的网关」。
# - 开发模式（npm run dev）：可不设置 VITE_API_URL，此时请求走相对路径并由 Vite 代理到 http://127.0.0.1:8000。
# - 若手工指定网关地址，Mac 上建议使用 http://127.0.0.1:8000（避免 localhost 走 IPv6 而后端仅监听 IPv4 导致 Failed to fetch）。
# - LocalAI（另一个可选后端）：常见默认 8080，对应 settings.localai_base_url，不是 Perilla 自己
# - Ollama：11434
# - Vite 前端开发服务器：5173
#
# VITE_API_URL=http://127.0.0.1:8000
