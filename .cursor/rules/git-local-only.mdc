---
description: 固化变更时用本地 git commit；除非用户明确要求，不要 git push；推送默认走 Gitee。
alwaysApply: true
---

# Git：仅本地提交（默认不推送）

- 用户要求固化结果时，可进行 **`git add` / `git commit`**。
- **不要**执行 **`git push`** 或其它推送到远端的操作，除非用户在对话中**明确**说要推送。
- 涉及 **`git rebase` / `git reset --hard`** 等改写历史的操作，仅在用户明确要求时执行。

# Git：默认推送远端（Gitee）

- 除非用户**另行指定**（例如「推到 GitHub」「只推 origin」），推送时以 **Gitee** 为主要远端：
  - 仓库：`https://gitee.com/yaolisi/perilla`
  - 远程名：`gitee`
- 用户要求推送时，优先执行 **`git push gitee`** 或 **`git push -u gitee <branch>`**（已配置 `remote.pushDefault=gitee` 时可用 **`git push`**）。
- **`origin`**（`https://github.com/yaolisi/perilla`）保留为镜像/备用远端，**不要**在用户未明确要求时推送到 GitHub。
