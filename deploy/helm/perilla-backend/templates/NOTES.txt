{{- $name := include "perilla-backend.fullname" . }}
{{- $ns := .Release.Namespace }}
已安装 {{ .Chart.Name }} chart（release={{ .Release.Name }}，namespace={{ $ns }}）。

常用命令：
  kubectl get pods -n {{ $ns }} -l app=perilla-backend
  kubectl rollout status deployment/{{ $name }} -n {{ $ns }}

HTTP 探针：存活 {{ .Values.probes.liveness.path }}，就绪 {{ .Values.probes.readiness.path }}
Prometheus 指标路径（与后端 PROMETHEUS_METRICS_PATH 对齐）：{{ .Values.metrics.path }}

{{- if or .Values.serviceAccount.create .Values.serviceAccount.name }}

ServiceAccount：{{ include "perilla-backend.serviceAccountName" . }}
{{- end }}
{{- if .Values.secretEnvFrom.enabled }}

环境变量 Secret（envFrom）：{{ .Values.secretEnvFrom.name }}（optional={{ .Values.secretEnvFrom.optional }}）
{{- end }}
{{- if .Values.ingress.enabled }}

Ingress 主机：{{ .Values.ingress.host }}（path={{ .Values.ingress.path }}）
{{- end }}

{{- if or .Values.networkPolicy.ingress.enabled .Values.networkPolicy.egress.enabled }}

NetworkPolicy 已启用（入站/出站）；请按 VPC/CIDR 核对 egress.privateCidr。
{{- end }}

terminationGracePeriodSeconds={{ .Values.terminationGracePeriodSeconds }}；若设置 env.uvicornTimeoutGracefulShutdownSeconds（映射 UVICORN_TIMEOUT_GRACEFUL_SHUTDOWN_SECONDS），须满足 宽限期 ≥ preStop({{ .Values.lifecycle.preStopSleepSeconds }}s) + 关停秒数 + 小余量。
可选 env：logFormat→LOG_FORMAT（默认 json）；logLevel→LOG_LEVEL；defaultModel→DEFAULT_MODEL；apiPrefix→API_PREFIX；trustedHosts→TRUSTED_HOSTS（逗号分隔 Host，支持 *.domain）；databaseUrl→DATABASE_URL（生产门禁 debug=false 必填 PostgreSQL）；dbConnectTimeoutSeconds→DB_CONNECT_TIMEOUT_SECONDS；dbPoolTimeoutSeconds→DB_POOL_TIMEOUT_SECONDS；dbStatementTimeoutMs→DB_STATEMENT_TIMEOUT_MS；dbPoolSize→DB_POOL_SIZE；dbMaxOverflow→DB_MAX_OVERFLOW；dbPoolRecycleSeconds→DB_POOL_RECYCLE_SECONDS；dbPath→DB_PATH（仅无 DATABASE_URL 的 SQLite）；redisClusterMode→REDIS_CLUSTER_MODE；inferenceCacheRedisUrl→INFERENCE_CACHE_REDIS_URL；inferenceCacheEnabled→INFERENCE_CACHE_ENABLED；ollamaBaseUrl→OLLAMA_BASE_URL；ollamaDefaultModel→OLLAMA_DEFAULT_MODEL；localaiBaseUrl→LOCALAI_BASE_URL；textgenWebuiBaseUrl→TEXTGEN_WEBUI_BASE_URL；eventBusEnabled→EVENT_BUS_ENABLED；eventBusBackend→EVENT_BUS_BACKEND；eventBusRedisUrl→EVENT_BUS_REDIS_URL；eventBusKafkaBootstrapServers→EVENT_BUS_KAFKA_BOOTSTRAP_SERVERS；prometheusEnabled→PROMETHEUS_ENABLED；prometheusMetricsPath→PROMETHEUS_METRICS_PATH（宜与 metrics.path 一致）；csrfCookieSecure→CSRF_COOKIE_SECURE；openapiPublicEnabled→OPENAPI_PUBLIC_ENABLED（仅内网需 Swagger 时设 true）；corsAllowedOrigins→CORS_ALLOWED_ORIGINS（浏览器前端域名白名单，逗号分隔）；apiRateLimitRedisUrl→API_RATE_LIMIT_REDIS_URL；apiRateLimitRedisFailClosed→API_RATE_LIMIT_REDIS_FAIL_CLOSED；healthReadyStrictApiRateLimitRedis→HEALTH_READY_STRICT_API_RATE_LIMIT_REDIS（须配 apiRateLimitRedisUrl）；healthReadyStrictEventBus→HEALTH_READY_STRICT_EVENT_BUS（须 EVENT_BUS_ENABLED 与总线 URL）；healthReadyStrictInferenceRedis→HEALTH_READY_STRICT_INFERENCE_REDIS（须推理缓存与 INFERENCE_CACHE_REDIS_URL）；httpMaxRequestBodyBytes→HTTP_MAX_REQUEST_BODY_BYTES；securityHeadersEnabled→SECURITY_HEADERS_ENABLED；securityHeadersXContentTypeOptions→SECURITY_HEADERS_X_CONTENT_TYPE_OPTIONS；securityHeadersXFrameOptions→SECURITY_HEADERS_X_FRAME_OPTIONS；securityHeadersReferrerPolicy→SECURITY_HEADERS_REFERRER_POLICY；securityHeadersStrictTransportSecurity→SECURITY_HEADERS_STRICT_TRANSPORT_SECURITY；uvicornTimeoutKeepAliveSeconds→UVICORN_TIMEOUT_KEEP_ALIVE_SECONDS。
/ready 探针减负（可选）：healthReadyInferenceRedisPingTimeoutSeconds→HEALTH_READY_INFERENCE_REDIS_PING_TIMEOUT_SECONDS；healthReadyInferenceRedisProbeEnabled→HEALTH_READY_INFERENCE_REDIS_PROBE_ENABLED；healthReadyInferenceRedisProbeCacheSeconds→HEALTH_READY_INFERENCE_REDIS_PROBE_CACHE_SECONDS；healthReadyApiRateLimitRedisProbeEnabled→HEALTH_READY_API_RATE_LIMIT_REDIS_PROBE_ENABLED；healthReadyApiRateLimitRedisProbeCacheSeconds→HEALTH_READY_API_RATE_LIMIT_REDIS_PROBE_CACHE_SECONDS。
治理与执行内核（可选）：executionKernelDbUrl→EXECUTION_KERNEL_DB_URL；securityGuardrailsStrict→SECURITY_GUARDRAILS_STRICT；trustedHostExemptOpsPaths→TRUSTED_HOST_EXEMPT_OPS_PATHS（false 时探针须匹配 Host）；forwardedAllowIps→FORWARDED_ALLOW_IPS（uvicorn 可信代理网段；Ingress 后常需配）；uvicornLimitConcurrency→UVICORN_LIMIT_CONCURRENCY；uvicornLimitMaxRequests→UVICORN_LIMIT_MAX_REQUESTS；uvicornServerHeader→UVICORN_SERVER_HEADER；uvicornH11MaxIncompleteEventSize→UVICORN_H11_MAX_INCOMPLETE_EVENT_SIZE；uvicornAccessLog→UVICORN_ACCESS_LOG；uvicornBacklog→UVICORN_BACKLOG；uvicornWsMaxSize→UVICORN_WS_MAX_SIZE；uvicornLimitMaxRequestsJitter→UVICORN_LIMIT_MAX_REQUESTS_JITTER；uvicornDateHeader→UVICORN_DATE_HEADER；uvicornWsPingIntervalSeconds→UVICORN_WS_PING_INTERVAL_SECONDS；uvicornWsPingTimeoutSeconds→UVICORN_WS_PING_TIMEOUT_SECONDS；uvicornTimeoutWorkerHealthcheckSeconds→UVICORN_TIMEOUT_WORKER_HEALTHCHECK_SECONDS；apiRateLimitEnabled→API_RATE_LIMIT_ENABLED；apiRateLimitRequests→API_RATE_LIMIT_REQUESTS；apiRateLimitWindowSeconds→API_RATE_LIMIT_WINDOW_SECONDS；apiRateLimitEventsRequests→API_RATE_LIMIT_EVENTS_REQUESTS（/api/events 专用窗口；与全局分流）；apiRateLimitEventsPathPrefix→API_RATE_LIMIT_EVENTS_PATH_PREFIX；apiRateLimitTrustXForwardedFor→API_RATE_LIMIT_TRUST_X_FORWARDED_FOR（直连公网建议 false；限流与审计 client_ip）；eventBusStrictStartup→EVENT_BUS_STRICT_STARTUP；requestTraceEnabled→REQUEST_TRACE_ENABLED。
感知与文件（可选）：imageGenerationDefaultModelId→IMAGE_GENERATION_DEFAULT_MODEL_ID；yoloModelPath→YOLO_MODEL_PATH；yoloDevice→YOLO_DEVICE（cpu|cuda|mps|auto）；yoloDefaultBackend→YOLO_DEFAULT_BACKEND；fileReadAllowedRoots→FILE_READ_ALLOWED_ROOTS；productionFileReadRequiredRoots→PRODUCTION_FILE_READ_REQUIRED_ROOTS；productionFileReadAllowedRoots→PRODUCTION_FILE_READ_ALLOWED_ROOTS；localModelDirectory→LOCAL_MODEL_DIRECTORY；autoUnloadLocalModelOnSwitch→AUTO_UNLOAD_LOCAL_MODEL_ON_SWITCH。
RBAC 与审计（可选）：rbacEnabled→RBAC_ENABLED；rbacEnforcement→RBAC_ENFORCEMENT；rbacDefaultRole→RBAC_DEFAULT_ROLE（生产不宜为 operator，匿名继承该角色；values 默认 viewer）；rbacAdminApiKeys→RBAC_ADMIN_API_KEYS；rbacOperatorApiKeys→RBAC_OPERATOR_API_KEYS；rbacViewerApiKeys→RBAC_VIEWER_API_KEYS（debug=false 且 rbacEnabled=true 时 validate_production_security_guardrails 要求至少一项非空且分段≥12 字符、非占位串）；auditLogEnabled→AUDIT_LOG_ENABLED；auditLogPathPrefixes→AUDIT_LOG_PATH_PREFIXES（勿单独 "/"）；auditLogIncludeGet→AUDIT_LOG_INCLUDE_GET。
API Key 与租户（可选）：apiKeyScopesJson→API_KEY_SCOPES_JSON；apiKeysJson→API_KEYS_JSON；apiKeyRevokedList→API_KEY_REVOKED_LIST；tenantEnforcementEnabled→TENANT_ENFORCEMENT_ENABLED；tenantDefaultId→TENANT_DEFAULT_ID；tenantHeaderName→TENANT_HEADER_NAME；tenantApiKeyBindingEnabled→TENANT_API_KEY_BINDING_ENABLED；tenantApiKeyTenantsJson→TENANT_API_KEY_TENANTS_JSON；csrfEnabled→CSRF_ENABLED；csrfHeaderName→CSRF_HEADER_NAME；csrfCookieName→CSRF_COOKIE_NAME；csrfCookiePath→CSRF_COOKIE_PATH；csrfCookieSamesite→CSRF_COOKIE_SAMESITE；csrfCookieMaxAgeSeconds→CSRF_COOKIE_MAX_AGE_SECONDS。
Trace 与脱敏（可选）：traceLinkEnabled→TRACE_LINK_ENABLED；apiRequestWhitelistEnabled→API_REQUEST_WHITELIST_ENABLED；dataRedactionEnabled→DATA_REDACTION_ENABLED；dataRedactionSensitiveFields→DATA_REDACTION_SENSITIVE_FIELDS；dataRedactionMaskKeepPrefix→DATA_REDACTION_MASK_KEEP_PREFIX；dataRedactionMaskKeepSuffix→DATA_REDACTION_MASK_KEEP_SUFFIX。
GZip 与工作流（可选）：responseGzipEnabled→RESPONSE_GZIP_ENABLED；responseGzipMinimumSize→RESPONSE_GZIP_MINIMUM_SIZE；workflowSchedulerMaxConcurrency→WORKFLOW_SCHEDULER_MAX_CONCURRENCY（1–256）。
工作流跨副本上限（可选，DB 软限制）：workflowDistributedRunningLimitEnabled→WORKFLOW_DISTRIBUTED_RUNNING_LIMIT_ENABLED；workflowDistributedRunningLimitPerWorkflow→WORKFLOW_DISTRIBUTED_RUNNING_LIMIT_PER_WORKFLOW；workflowDistributedRunningLimitWaitSeconds→WORKFLOW_DISTRIBUTED_RUNNING_LIMIT_WAIT_SECONDS；workflowDistributedRunningLimitFailOpen→WORKFLOW_DISTRIBUTED_RUNNING_LIMIT_FAIL_OPEN；workflowDistributedRunningStaleSeconds→WORKFLOW_DISTRIBUTED_RUNNING_STALE_SECONDS；workflowDistributedRunningAutoReconcileStale→WORKFLOW_DISTRIBUTED_RUNNING_AUTO_RECONCILE_STALE。
工作流发布/执行策略（可选）：workflowExecutionWaitDefault→WORKFLOW_EXECUTION_WAIT_DEFAULT；workflowAllowDraftExecution→WORKFLOW_ALLOW_DRAFT_EXECUTION（生产建议 false）；workflowAllowDraftExecutionDebugOverride→WORKFLOW_ALLOW_DRAFT_EXECUTION_DEBUG_OVERRIDE（仅 debug 路径）。
工作流子流程治理（可选）：workflowSubworkflowMaxDepth→WORKFLOW_SUBWORKFLOW_MAX_DEPTH；workflowAllowLatestSubworkflowInProduction→WORKFLOW_ALLOW_LATEST_SUBWORKFLOW_IN_PRODUCTION（生产建议 false）；workflowBlockPublishOnSubworkflowBreakingImpact→WORKFLOW_BLOCK_PUBLISH_ON_SUBWORKFLOW_BREAKING_IMPACT（建议 true）。
工作流契约策略（可选）：workflowContractRequiredInputAddedBreaking→WORKFLOW_CONTRACT_REQUIRED_INPUT_ADDED_BREAKING；workflowContractOutputAddedRisky→WORKFLOW_CONTRACT_OUTPUT_ADDED_RISKY；workflowContractFieldExemptions→WORKFLOW_CONTRACT_FIELD_EXEMPTIONS（逗号分隔字段豁免）。
工作流 DB 写重试与反思（可选）：workflowDbWriteRetryAttempts→WORKFLOW_DB_WRITE_RETRY_ATTEMPTS；workflowDbWriteRetryBaseDelayMs→WORKFLOW_DB_WRITE_RETRY_BASE_DELAY_MS；workflowReflectorMaxRetries→WORKFLOW_REFLECTOR_MAX_RETRIES；workflowReflectorRetryIntervalSeconds→WORKFLOW_REFLECTOR_RETRY_INTERVAL_SECONDS；workflowReflectorFallbackAgentId→WORKFLOW_REFLECTOR_FALLBACK_AGENT_ID。
工作流等待与治理（可选）：workflowWaitTimeoutSeconds→WORKFLOW_WAIT_TIMEOUT_SECONDS；workflowWaitTimeoutMaxSeconds→WORKFLOW_WAIT_TIMEOUT_MAX_SECONDS；workflowGovernanceHealthyThreshold→WORKFLOW_GOVERNANCE_HEALTHY_THRESHOLD；workflowGovernanceWarningThreshold→WORKFLOW_GOVERNANCE_WARNING_THRESHOLD；workflowPendingWarnSeconds→WORKFLOW_PENDING_WARN_SECONDS；workflowPendingWarnIntervalSeconds→WORKFLOW_PENDING_WARN_INTERVAL_SECONDS。
Workflow approvals 弃用响应（可选）：workflowApprovalsLegacyDeprecatedHeader→WORKFLOW_APPROVALS_LEGACY_DEPRECATED_HEADER；workflowApprovalsLegacySunset→WORKFLOW_APPROVALS_LEGACY_SUNSET（HTTP-date）。
Chat 异步与 SSE（可选）：asyncChatJobTtlSeconds→ASYNC_CHAT_JOB_TTL_SECONDS；asyncChatJobMaxEntries→ASYNC_CHAT_JOB_MAX_ENTRIES；chatStreamResumeEnabled→CHAT_STREAM_RESUME_ENABLED；chatStreamResumeTtlSeconds→CHAT_STREAM_RESUME_TTL_SECONDS；chatStreamResumeMaxSessions→CHAT_STREAM_RESUME_MAX_SESSIONS；chatStreamResumeWaitTimeoutSeconds→CHAT_STREAM_RESUME_WAIT_TIMEOUT_SECONDS；chatStreamWallClockMaxSeconds→CHAT_STREAM_WALL_CLOCK_MAX_SECONDS（0=不限制）；chatStreamResumeCancelUpstreamOnDisconnect→CHAT_STREAM_RESUME_CANCEL_UPSTREAM_ON_DISCONNECT。
Chat 会话与幂等、清洗（可选）：chatSessionReuseWindowMinutes→CHAT_SESSION_REUSE_WINDOW_MINUTES；chatSessionTitleMaxLen→CHAT_SESSION_TITLE_MAX_LEN；chatIdempotencyHeaders→CHAT_IDEMPOTENCY_HEADERS；chatForceNewSessionHeaders→CHAT_FORCE_NEW_SESSION_HEADERS；chatInputStripTransportWrappers→CHAT_INPUT_STRIP_TRANSPORT_WRAPPERS；chatOutputStripReasoning→CHAT_OUTPUT_STRIP_REASONING。
长期记忆（可选）：enableLongTermMemory→ENABLE_LONG_TERM_MEMORY；memoryInjectMode→MEMORY_INJECT_MODE（recent|keyword|vector）；memoryInjectTopK→MEMORY_INJECT_TOP_K；memoryVectorEnabled→MEMORY_VECTOR_ENABLED；memoryEmbeddingDim→MEMORY_EMBEDDING_DIM；memoryDefaultConfidence→MEMORY_DEFAULT_CONFIDENCE；memoryMergeEnabled→MEMORY_MERGE_ENABLED；memoryMergeSimilarityThreshold→MEMORY_MERGE_SIMILARITY_THRESHOLD；memoryConflictEnabled→MEMORY_CONFLICT_ENABLED；memoryConflictSimilarityThreshold→MEMORY_CONFLICT_SIMILARITY_THRESHOLD；memoryDecayHalfLifeDays→MEMORY_DECAY_HALF_LIFE_DAYS；memoryKeySchemaEnforced→MEMORY_KEY_SCHEMA_ENFORCED；memoryKeySchemaAllowUnlisted→MEMORY_KEY_SCHEMA_ALLOW_UNLISTED；memoryExtractorEnabled→MEMORY_EXTRACTOR_ENABLED；memoryExtractorTemperature→MEMORY_EXTRACTOR_TEMPERATURE；memoryExtractorTopP→MEMORY_EXTRACTOR_TOP_P；memoryExtractorMaxTokens→MEMORY_EXTRACTOR_MAX_TOKENS。
推理队列与看板（可选）：inferenceQueueSloEnabled→INFERENCE_QUEUE_SLO_ENABLED；inferenceQueueSloHighMs→INFERENCE_QUEUE_SLO_HIGH_MS；inferenceQueueSloMediumMs→INFERENCE_QUEUE_SLO_MEDIUM_MS；inferenceQueueSloLowMs→INFERENCE_QUEUE_SLO_LOW_MS；inferenceQueuePreemptionEnabled→INFERENCE_QUEUE_PREEMPTION_ENABLED；inferenceQueuePreemptionMaxPerHighRequest→INFERENCE_QUEUE_PREEMPTION_MAX_PER_HIGH_REQUEST；inferenceQueuePreemptionMaxPerTask→INFERENCE_QUEUE_PREEMPTION_MAX_PER_TASK；inferenceQueuePreemptionCooldownMs→INFERENCE_QUEUE_PREEMPTION_COOLDOWN_MS；inferencePriorityPanelHighSloCriticalRate→INFERENCE_PRIORITY_PANEL_HIGH_SLO_CRITICAL_RATE；inferencePriorityPanelHighSloWarningRate→INFERENCE_PRIORITY_PANEL_HIGH_SLO_WARNING_RATE；inferencePriorityPanelPreemptionCooldownBusyThreshold→INFERENCE_PRIORITY_PANEL_PREEMPTION_COOLDOWN_BUSY_THRESHOLD。
连续批（可选）：continuousBatchEnabled→CONTINUOUS_BATCH_ENABLED；continuousBatchWaitMs→CONTINUOUS_BATCH_WAIT_MS；continuousBatchMaxSize→CONTINUOUS_BATCH_MAX_SIZE。
运行时并发与 Agent（可选）：runtimeMaxConcurrencyOverrides→RUNTIME_MAX_CONCURRENCY_OVERRIDES（JSON）；agentPlanMaxParallelSteps→AGENT_PLAN_MAX_PARALLEL_STEPS；agentStepDefaultTimeoutSeconds→AGENT_STEP_DEFAULT_TIMEOUT_SECONDS；agentStepDefaultMaxRetries→AGENT_STEP_DEFAULT_MAX_RETRIES；agentStepDefaultRetryIntervalSeconds→AGENT_STEP_DEFAULT_RETRY_INTERVAL_SECONDS。
推理智能路由（可选）：inferenceSmartRoutingEnabled→INFERENCE_SMART_ROUTING_ENABLED；inferenceSmartRoutingPoliciesJson→INFERENCE_SMART_ROUTING_POLICIES_JSON。
缓存与知识库分块（可选）：inferenceCacheTtlByModelTypeJson→INFERENCE_CACHE_TTL_BY_MODEL_TYPE_JSON；kbChunkSizeOverridesJson→KB_CHUNK_SIZE_OVERRIDES_JSON。
推理缓存标量（可选）：inferenceCacheTtlSeconds→INFERENCE_CACHE_TTL_SECONDS；embeddingCacheTtlSeconds→EMBEDDING_CACHE_TTL_SECONDS；inferenceCachePrefix→INFERENCE_CACHE_PREFIX；inferenceCacheMemoryEnabled→INFERENCE_CACHE_MEMORY_ENABLED；inferenceCacheMemoryMaxEntries→INFERENCE_CACHE_MEMORY_MAX_ENTRIES。
推理缓存全量清理挑战（可选）：inferenceCacheClearChallengeTtlSeconds→INFERENCE_CACHE_CLEAR_CHALLENGE_TTL_SECONDS；inferenceCacheClearChallengeRateWindowSeconds→INFERENCE_CACHE_CLEAR_CHALLENGE_RATE_WINDOW_SECONDS；inferenceCacheClearChallengeRateMaxPerWindow→INFERENCE_CACHE_CLEAR_CHALLENGE_RATE_MAX_PER_WINDOW。
{{- if .Values.migrateJob.enabled }}

迁移 Job 已启用（镜像与 backend 一致）：kubectl wait --for=condition=complete job/{{ include "perilla-backend.fullname" . }}-migrate -n {{ $ns }} --timeout=300s
{{- end }}

静态 YAML 参考：deploy/k8s/*.example.yaml；默认值：deploy/helm/perilla-backend/values.yaml
