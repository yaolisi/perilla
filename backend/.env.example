# =============================================================================
# 本地 AI 推理平台 - 环境变量示例
# 复制为 .env 并按需修改
#
# 推荐：通过 Platform Settings UI 配置（无需重启）
# 以下仅用于部署/CI 时覆盖默认值
# =============================================================================

# 本地模型根目录（LLM/VLM/Embedding/ASR/Perception 均在此下）
# LOCAL_MODEL_DIRECTORY=~/.local-ai/models/

# 其他配置见 config/settings.py
