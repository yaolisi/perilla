# =============================================================================
# OpenVitamin 后端依赖（macOS / Apple Silicon）
# 建议在 conda 虚拟环境中安装：pip install -r requirements/macos.txt
# =============================================================================

-r base.txt

# --- MLX / Apple Silicon 文生图与本地推理 ---
mlx>=0.0.1
mlx-lm>=0.20.0
mflux>=0.16.0
